{
  "name": "nestx-server",
  "private": true,
  "scripts": {
    "start:serve": "cd packages/server && npm start",
    "gen": "cd packages/server && npm run swagger"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "dependencies": {
    "nestx-common": "0.0.5",
    "nestx-config": "0.0.5"
  }
}
